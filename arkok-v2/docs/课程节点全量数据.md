# 课程节点全量数据架构设计

## 真实使用场景

**托管班下午 5-8 点集中辅导**

```
17:00 - 20:00 集中辅导时段
│
├── 📝 主要任务：辅导作业 + 完成过关
│   └── 老师巡视检查，学生依次过关
│
├── ⏱️ 少量时间：知识点讲解
│   └── 针对普遍错误集中讲解
│
└── 📊 错题统计：页数+题号
    └── 例：48页第3题，错1次
    └── 无需拍照，定位清晰即可重做
```

---

## 核心设计理念

**课程节点（Unit + Lesson）是所有教学数据的核心锚点**

每个课程节点作为标记，承载该课相关的所有教学记录，包括：
- 基础过关任务
- 辅导/补过记录
- 错题及重做记录（页数+题号）
- 知识点讲解记录


---

## 数据架构

```
📍 课程节点 (subject: "chinese", unit: "6", lesson: "20")
   标题: "精彩极了"和"糟糕透了"
│
├── 📋 基础过关项 (QC Tasks)
│   ├── 生字听写 [✓] X2 (补过2次)
│   ├── 课文背诵 [✓]
│   ├── 古诗默写 [ ]
│   └── [自定义项...] (老师新增)
│
├── 📝 辅导/补过记录 (Tutoring Log)
│   ├── attempts: 累计辅导次数
│   ├── 每次辅导时间戳
│   └── 最终是否过关
│
├── ❌ 错题记录 (Mistakes)
│   ├── 错题内容
│   ├── 错误答案 / 正确答案
│   ├── 重做次数 (retryCount)
│   ├── 最近重做时间
│   └── 状态: pending / mastered
│
├── 💡 知识点讲解 (Knowledge Points)
│   ├── 知识点名称
│   ├── 讲解时间
│   ├── 讲解方式 (口述/板书/视频等)
│   └── 学生理解程度评估
│
└── 📊 知识点测验 (Quiz)
    ├── 测验标题
    ├── 得分 / 满分
    ├── 错题关联
    └── 测验时间
```

---

## 当前实现状态

| 功能模块 | 状态 | 说明 |
|---------|------|------|
| 基础过关项 | ✅ 已实现 | 支持默认项+自定义项 |
| 辅导记录 (Xn) | ✅ 已实现 | attempts 字段存储 |
| 错题记录 | 📋 待实现 | 需要 mistakes 表扩展 |
| 错题重做次数 | 📋 待实现 | retryCount 字段 |
| 知识点讲解 | 📋 待实现 | 新增业务模块 |
| 知识点测验 | 📋 待实现 | 关联测验系统 |

---

## 数据库设计建议

### 1. task_records 表（已有）
```typescript
{
  id: string;
  studentId: string;
  type: 'QC' | 'TASK' | 'SPECIAL';
  title: string;
  status: 'PENDING' | 'COMPLETED';
  attempts: number;  // 辅导次数
  content: {
    unit: string;     // 课程节点锚点
    lesson: string;   // 课程节点锚点
    category: string;
    ...
  }
}
```

### 2. mistakes 表（轻量级设计）

> **设计理念**：只记录定位信息（页数+题号），无需存储原题内容，避免拍照等重开发

> **页面位置**：个人详情页 → 错题本 Tab

```typescript
{
  id: string;
  studentId: string;
  subject: 'chinese' | 'math' | 'english';
  unit: string;       // 课程节点锚点
  lesson: string;     // 课程节点锚点
  // 📍 定位信息（足够老师找到原题）
  workbookPage: number;  // 作业册页数
  questionNo: number;    // 第几题
  // 🏷️ 错因分析标签
  errorCause: string;    // 错因标签：审题不清/计算失误/概念混淆/粗心大意/知识盲区
  // 📊 统计信息
  wrongCount: number;    // 这道题错了几次
  retryCount: number;    // 重做了几次
  lastRetryAt: Date;
  status: 'pending' | 'mastered';
}
```

**错因标签预设**：
| 标签 | 说明 |
|-----|------|
| 审题不清 | 没看清题目要求 |
| 计算失误 | 计算过程出错 |
| 概念混淆 | 知识点理解错误 |
| 粗心大意 | 会做但做错了 |
| 知识盲区 | 完全不会 |

**UI 呈现位置**：
```
学生详情页 (StudentDetail)
├── 🏠 基础信息
├── 📊 学业攻克
├── ❌ 错题本 ← 新增 Tab
│   ├── [语文] [数学] [英语] ← 科目切换
│   ├── 按课程节点分组
│   │   ├── 第6单元第20课 (3道错题)
│   │   │   ├── P48 第3题 [计算失误] 重做1次 ✓
│   │   │   └── P52 第7题 [概念混淆] 待重做
│   │   └── 第5单元第15课 (1道错题)
│   └── [+ 添加错题] 按钮
└── 📈 成长轨迹
```


**使用场景**：

- 老师发现小明第48页第3题错了，点击记录
- 系统存储：`page: 48, question: 3, wrongCount: 1`
- 复习时：老师说"翻到48页第3题重做"
- 学生做对后：`retryCount: 1, status: mastered`


### 3. knowledge_points 表（待新增）
```typescript
{
  id: string;
  studentId: string;
  subject: string;
  unit: string;       // 课程节点锚点
  lesson: string;     // 课程节点锚点
  pointName: string;
  taughtAt: Date;
  method: string;     // 讲解方式
  comprehension: number; // 理解程度 1-5
}
```

### 4. quizzes 表（待新增）
```typescript
{
  id: string;
  studentId: string;
  subject: string;
  unit: string;       // 课程节点锚点
  lesson: string;     // 课程节点锚点
  title: string;
  score: number;
  maxScore: number;
  mistakeIds: string[]; // 关联错题
  createdAt: Date;
}
```

---

## 关键实现原则

1. **所有记录必须包含 unit + lesson 字段**
   - 这是数据归属的核心标识

2. **切换课程节点 = 查看该节点下所有数据**
   - 前端过滤逻辑统一使用 unit/lesson 匹配

3. **历史节点数据永久保留**
   - 切换进度只是视图切换，不删除数据
   - 随时可切回历史节点查看完整记录

4. **数据聚合查看**
   - 可按节点聚合展示学生该课学习全貌
   - 支持横向对比不同课程的学习情况
