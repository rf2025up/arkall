# 🌐 Growark + StarJourney v13.0 公网部署完整指南

**文档版本**: v1.0
**创建时间**: 2025-12-10
**最后更新**: 2025-12-10
**适用版本**: Growark + StarJourney v13.0 融合系统

---

## 🎯 部署概览

### 系统架构
```
🌐 公网访问: https://esboimzbkure.sealosbja.site
    ↓
Sealos Devbox 内网穿透
    ↓
┌─────────────────────────────────────┐
│       双服务器架构                │
├─────────────────┬───────────────────┤
│   端口 3000     │     端口 3001      │
│   Growark       │   StarJourney     │
│   (主服务)      │   (学情管理)      │
└─────────────────┴───────────────────┘
    ↓                     ↓
┌─────────────────────────────────────┐
│        PostgreSQL 数据库            │
│  • students, teams, groups         │
│  • challenges, pk_matches, tasks   │
│  • lms_mistakes, lms_records       │
│  • lms_academic_reports            │
└─────────────────────────────────────┘
```

### 核心功能
- **5Tab底部导航**: [班级][备课]📷[质检][我的]
- **StarJourney学情管理**: 三Tab界面 (学情概览 + 成长管理 + 学业分析)
- **完整功能模块**: 积分、挑战、PK、勋章、习惯、过关管理
- **实时数据同步**: WebSocket + HTTP轮询
- **移动端适配**: 响应式设计，触摸操作优化

---

## 🚀 快速部署指南

### 第一次部署

**1. 环境检查**
```bash
# 确认项目目录
cd /home/devbox/project
ls -la arkok/server.js starj/star-server.js

# 检查数据库配置
cat arkok/.env
```

**2. 启动服务**
```bash
# 启动集成系统
./start-integrated-system.sh
```

**3. 验证部署**
```bash
# 本地验证
curl http://localhost:3000/health
curl http://localhost:3001/api/health

# 公网验证
curl https://esboimzbkure.sealosbja.site/health
```

### 新功能开发后部署

**重要**: 每次新功能开发完成后，必须执行以下步骤确保公网用户能看到更新！

**步骤1: 前端构建**
```bash
cd arkok/mobile
npm run build
```

**步骤2: 部署到生产环境**
```bash
# 方法1: 使用自动化脚本 (推荐)
bash scripts/部署到生产环境.sh

# 方法2: 手动部署
cp -r mobile/dist/* ../public/
```

**步骤3: 验证部署**
```bash
# 验证页面可访问
curl -s -o /dev/null -w "%{http_code}" https://esboimzbkure.sealosbja.site/app

# 验证API正常
curl -s -o /dev/null -w "%{http_code}" https://esboimzbkure.sealosbja.site/health
```

**步骤4: 功能验证**
```bash
# 执行完整功能验证
bash scripts/功能验证脚本.sh
```

---

## 📱 公网访问地址 (已确认)

### ✅ 正确的公网域名
**基础地址**: `https://esboimzbkure.sealosbja.site`

### 🔗 访问路径映射
| 功能模块 | 公网地址 | 状态说明 |
|---------|----------|----------|
| 📱 **手机端(教师端)** | https://esboimzbkure.sealosbja.site/app | ✅ **主要入口** |
| 📺 **大屏端** | https://esboimzbkure.sealosbja.site/screen | ✅ 数据展示 |
| 👤 **学生端** | https://esboimzbkure.sealosbja.site/student | ✅ 学生界面 |
| 📊 **API接口** | https://esboimzbkure.sealosbja.site/api/* | ✅ 数据接口 |
| ❤️ **健康检查** | https://esboimzbkure.sealosbja.site/health | ✅ 服务监控 |

### 📱 核心功能使用方法

#### StarJourney学情管理 (v13.0核心功能)
1. **访问**: https://esboimzbkure.sealosbja.site/app
2. **打开方式**:
   - 方法1: 点击学生卡片 → 评分面板 → 学情管理 📖
   - 方法2: 双击学生卡片 (300ms内) → 直接打开
   - 方法3: 点击右上角 📖 学情管理按钮 → 选择学生
   - 方法4: 点击学生卡片右下角蓝色用户图标 👤
3. **三Tab界面**:
   - 📊 **学情概览**: 班级整体状态、关注学生、数据统计
   - 📈 **成长管理**: 任务创建、过关记录、错题管理
   - 📚 **学业分析**: 个人数据、辅导跟踪、智能分析

#### 5Tab导航功能
- **班级**: 学生管理、积分发放、挑战PK
- **备课**: 📝 学科切换、任务管理、计划发布 (v13.0新增)
- **质检**: 🛡️ 辅导计数、学生管理、批量操作 (v13.0新增)
- **我的**: ⚙️ 设置页面、个人配置、系统管理 (v13.0整合)

---

## ⚙️ 配置文件说明

### 1. API配置 (硬编码正确地址)
```typescript
// arkok/mobile/services/api.ts
const API_BASE_URL = 'https://esboimzbkure.sealosbja.site/api';
const STARJOURNEY_API_BASE_URL = 'https://esboimzbkure.sealosbja.site/api';
```

### 2. 启动脚本配置
```bash
# start-integrated-system.sh - 第105-113行
echo "🌐 公网访问地址:"
echo "   ✅ 已确认: https://esboimzbkure.sealosbja.site"
echo "🔗 公网访问路径:"
echo "   📱 管理端:     https://esboimzbkure.sealosbja.site/app"
echo "   📺 大屏端:     https://esboimzbkure.sealosbja.site/screen"
echo "   👤 学生端:     https://esboimzbkure.sealosbja.site/student"
```

### 3. 数据库配置
```bash
# arkok/.env
DB_HOST=growark-postgresql.ns-bg6fgs6y.svc
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=kngwb5cb
```

---

## 🔧 部署工具和脚本

### 1. 启动脚本
**文件**: `./start-integrated-system.sh`
**功能**: 同时启动Growark(3000)和StarJourney(3001)服务器
```bash
# 后台启动
nohup ./start-integrated-system.sh > integrated.log 2>&1 &

# 查看运行状态
ps aux | grep -E "(arkok/server|star-server)" | grep -v grep
```

### 2. 自动部署脚本
**文件**: `./scripts/部署到生产环境.sh`
**功能**: 构建前端代码 → 部署到生产目录 → 验证部署结果
```bash
bash scripts/部署到生产环境.sh
```

### 3. 功能验证脚本
**文件**: `./scripts/功能验证脚本.sh`
**功能**: 全面验证系统功能、API接口、页面访问
```bash
bash scripts/功能验证脚本.sh
```

---

## 📋 部署检查清单

### ✅ 部署前检查
- [ ] 代码修改已保存
- [ ] API地址配置正确 (esboimzbkure.sealosbja.site)
- [ ] 数据库连接配置正确
- [ ] 无TODO占位符代码
- [ ] 本地测试通过

### ✅ 部署步骤执行
- [ ] 前端构建成功 (`npm run build`)
- [ ] 构建产物部署成功 (`cp -r mobile/dist/* public/`)
- [ ] 服务器状态正常 (双服务运行)
- [ ] 健康检查通过 (返回200状态码)

### ✅ 部署后验证
- [ ] 手机端页面访问正常
- [ ] API接口响应正常
- [ ] 新功能交互正常
- [ ] 用户数据同步正常
- [ ] 移动端兼容性正常

---

## 🚨 常见问题与解决方案

### Q1: 公网无法访问
**现象**: 页面显示"准备中"或连接超时
**解决**:
```bash
# 1. 检查服务运行状态
ps aux | grep -E "(arkok|starj)" | grep -v grep

# 2. 检查端口监听
netstat -tulpn | grep -E ":300[01]"

# 3. 验证本地服务
curl http://localhost:3000/health

# 4. 检查公网地址
curl https://esboimzbkure.sealosbja.site/health
```

### Q2: 新功能不可见 (用户刷新后看不到更新)
**根本原因**: 前端构建产物未部署到生产环境
**解决**:
```bash
# 1. 重新构建
cd arkok/mobile && npm run build

# 2. 部署新构建产物
cp -r mobile/dist/* ../public/

# 3. 清理浏览器缓存 (用户端)
# Chrome: Ctrl+Shift+R / Safari: Cmd+Shift+R

# 4. 验证部署
curl https://esboimzbkure.sealosbja.site/app
```

### Q3: API接口调用失败
**现象**: 数据加载失败，错误提示
**解决**:
```bash
# 1. 检查API地址配置
grep -r "esboimzbkure.sealosbja.site" mobile/services/api.ts

# 2. 测试API接口
curl https://esboimzbkure.sealosbja.site/api/students

# 3. 检查服务器日志
tail -f growark.log
tail -f starjourney.log
```

### Q4: 页面显示空白或404错误
**现象**: 访问页面白屏，控制台404错误
**解决**:
```bash
# 1. 检查构建产物
ls -la mobile/dist/
ls -la public/assets/main-*.js

# 2. 重新部署构建产物
rm -rf public/assets/*
cp -r mobile/dist/* public/

# 3. 验证文件访问
curl -I https://esboimzbkure.sealosbja.site/app
```

### Q5: 数据库连接问题
**现象**: 无法读取学生数据，API返回错误
**解决**:
```bash
# 1. 检查数据库配置
cat arkok/.env

# 2. 测试数据库连接
psql -h growark-postgresql.ns-bg6fgs6y.svc -U postgres -d postgres -c "\dt"

# 3. 重启服务
./start-integrated-system.sh
```

---

## 📈 性能监控

### 关键指标
- **API响应时间**: < 100ms (目标)
- **页面加载时间**: < 3秒 (目标)
- **错误率**: < 1% (目标)
- **并发用户数**: 支持50+同时访问

### 监控命令
```bash
# 1. 服务状态监控
while true; do
  STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://esboimzbkure.sealosbja.site/health)
  if [ "$STATUS" != "200" ]; then
    echo "⚠️ 服务异常，状态码: $STATUS" | tee -a monitor.log
  fi
  sleep 30
done

# 2. API性能测试
time curl -s https://esboimzbkure.sealosbja.site/api/students > /dev/null

# 3. 服务器资源监控
top -p $(pgrep -f "arkok/server") -p $(pgrep -f "star-server")
```

---

## 🔄 维护和更新

### 日常维护
```bash
# 1. 查看服务日志
tail -f growark.log starjourney.log

# 2. 检查磁盘空间
df -h

# 3. 数据库备份
pg_dump -h growark-postgresql.ns-bg6fgs6y.svc -U postgres -d postgres > backup-$(date +%Y%m%d).sql
```

### 更新流程
1. **开发阶段**: 修改代码 → 本地测试
2. **构建阶段**: `npm run build`
3. **部署阶段**: `bash scripts/部署到生产环境.sh`
4. **验证阶段**: `bash scripts/功能验证脚本.sh`
5. **用户测试**: 多浏览器测试新功能

### 回滚方案
```bash
# 1. 停止服务
kill $(pgrep -f "arkok/server") $(pgrep -f "star-server")

# 2. 恢复上一个版本
git checkout HEAD~1
npm run build
cp -r mobile/dist/* public/

# 3. 重启服务
./start-integrated-system.sh
```

---

## 📞 技术支持

### 相关文档
- **项目进度**: `/home/devbox/project/star-融合进度跟踪文档.md`
- **部署状态**: `/home/devbox/project/公网部署状态报告.md`
- **启动指南**: `/home/devbox/project/启动公网.md`
- **开发规范**: `/home/devbox/project/开发规范-功能验证与文档同步机制.md`

### 联系信息
- **技术支持**: Claude Code AI助手
- **文档更新**: 每次部署后自动更新
- **问题反馈**: 通过部署报告记录

### 紧急联系
```bash
# 服务异常处理
kill $(pgrep -f "arkok/server") $(pgrep -f "star-server")
./start-integrated-system.sh

# 数据库问题
psql -h growark-postgresql.ns-bg6fgs6y.svc -U postgres -d postgres
```

---

## 🎉 部署成功确认

### 最终验证清单
- [x] ✅ 双服务器启动成功 (3000 + 3001)
- [x] ✅ 公网地址确认 (esboimzbkure.sealosbja.site)
- [x] ✅ API接口响应正常
- [x] ✅ 手机端页面访问正常
- [x] ✅ 5Tab导航功能完整
- [x] ✅ StarJourney学情管理可用
- [x] ✅ 实时数据同步正常
- [x] ✅ 部署脚本和工具完善

### 用户访问指引
**主要入口**: https://esboimzbkure.sealosbja.site/app

**功能特色**:
- 📱 现代化5Tab底部导航设计
- 🎯 完整的StarJourney学情管理系统
- 🔄 实时数据同步和状态更新
- 📊 丰富的数据统计和可视化
- 🛡️ 稳定的系统架构和性能保障

---

**🎯 部署完成！系统已成功上线，用户可通过公网地址访问全部功能！**

**最后更新**: 2025-12-10
**版本**: v13.0 融合系统
**状态**: ✅ 生产就绪