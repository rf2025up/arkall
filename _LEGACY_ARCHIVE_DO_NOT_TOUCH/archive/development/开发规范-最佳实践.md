# StarJourney与Growark集成最佳实践

## 🚨 重要：公网部署最佳实践 (2025-12-10更新)

### 问题总结
今天在功能修改过程中多次出现公网更新问题，主要表现为：
1. 修改功能后没有正确更新到公网环境
2. 页面显示空白或404错误
3. 资源文件版本不匹配

### ✅ 最佳实践流程

#### 1. 开发环境要求
- **工作目录**：始终确保在 `/home/devbox/project` 根目录操作
- **源码位置**：
  - 手机端源码：`/home/devbox/project/arkok/mobile/`
  - 大屏端源码：`/home/devbox/project/arkok/bigscreen/`
  - 服务端代码：`/home/devbox/project/arkok/server.js`

#### 2. 代码修改流程
```bash
# 1. 切换到项目根目录
cd /home/devbox/project

# 2. 修改源码文件（手机端）
# 编辑 /home/devbox/project/arkok/mobile/src/ 下的文件

# 3. 确保依赖已安装
cd /home/devbox/project/arkok/mobile
npm install
```

#### 3. 构建与部署流程
```bash
# 方法1：使用快速部署脚本（推荐）
cd /home/devbox/project
./快速部署.sh

# 方法2：手动构建部署
cd /home/devbox/project/arkok/mobile
npm run build
cp dist/assets/main-*.js ../public/assets/
cp dist/index.html ../public/
cd /home/devbox/project
./entrypoint.sh production
```

#### 4. 关键检查点
```bash
# 检查构建文件是否存在
ls -lh /home/devbox/project/arkok/public/assets/main-*.js

# 检查服务是否运行
ps aux | grep "arkok/server.js"

# 检查服务健康状态
curl http://localhost:3000/health

# 检查端口监听
netstat -tulpn | grep :3000
```

#### 5. 版本管理
- **版本标识**：自动使用时间戳格式 `vYYYYMMDDHHMM`
- **缓存清理**：每次构建自动更新版本号，确保资源不缓存
- **回滚机制**：保留上一个版本的文件在 `/public/assets/` 目录

#### 6. 服务管理
```bash
# 停止旧服务
kill $(pgrep -f "arkok/server.js")

# 启动新服务
./entrypoint.sh production

# 后台运行（推荐）
nohup ./entrypoint.sh production > server.log 2>&1 &

# 查看日志
tail -f server.log
```

### ⚠️ 常见问题与解决方案

#### Q1: 手机端显示空白页
**根本原因**：`public/assets/` 目录缺少 `main-*.js` 文件
**解决方法**：
```bash
cd /home/devbox/project/arkok/mobile
npm run build
cp dist/assets/main-*.js ../public/assets/
```

#### Q2: 修改后没有生效
**可能原因**：
1. 没有重新构建前端代码
2. 没有重启服务器
3. 浏览器缓存问题

**解决方法**：
1. 强制刷新浏览器 (Ctrl+F5)
2. 检查版本号是否更新
3. 重新执行完整部署流程

#### Q3: 公网地址显示"准备中"
**解决方法**：
1. 等待2-5分钟（网关准备时间）
2. 检查服务是否监听 `0.0.0.0:3000`
3. 确认服务健康检查通过

### 📋 完整部署检查清单
- [ ] 代码修改完成
- [ ] 前端构建成功 (`npm run build`)
- [ ] 资源文件复制完成 (`cp dist/assets/main-*.js ../public/assets/`)
- [ ] 服务器停止 (`kill $(pgrep -f "arkok/server.js")`)
- [ ] 服务器启动 (`./entrypoint.sh production`)
- [ ] 健康检查通过 (`curl http://localhost:3000/health`)
- [ ] 公网访问测试通过

### 🌐 公网访问地址
- **当前地址**：`https://esboimzbkure.sealosbja.site`
- **手机端**：`https://esboimzbkure.sealosbja.site/app`
- **大屏端**：`https://esboimzbkure.sealosbja.site/screen`
- **API检查**：`https://esboimzbkure.sealosbja.site/api/students`

### 📝 今天完成的功能更新
1. **班级首页评分功能修改**：隐藏预制加分项目，保留手动输入
2. **QC质检台页面重构**：完整的质检台和结算台双标签页功能
3. **Prep备课页面重构**：动态输入字段、选择器、个性化任务功能
4. **StarJourneyModal UI升级**：全能学生档案卡（成长激励 + 学业攻克）

---

**最后更新**：2025-12-10 11:25
**文档版本**：v1.0
**适用项目**：arkok (StarJourney + Growark 集成系统)